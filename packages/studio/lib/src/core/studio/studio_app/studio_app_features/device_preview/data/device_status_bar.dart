import 'package:flutter/material.dart';

class DeviceStatusBar {
  static const Color defaultColor = Color(0x40000000);

  final Color color;

  const DeviceStatusBar({
    this.color = defaultColor,
  });
}
